<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:data="clr-namespace:Rivals2Tracker.Models"
                    xmlns:converters="clr-namespace:Rivals2Tracker.Resources.Converters">

    <converters:IconConverter x:Key="IconConverter" />
    <converters:WinLoseColorConverter x:Key="WinLoseColorConverter" />
    <converters:WinRatioToBackgroundConverter x:Key="WinRatioToColor" />

    <DataTemplate x:Key="MatchResultsTemplate" DataType="{x:Type data:MatchResult}">
        <Grid Height="55">
            <StackPanel Orientation="Horizontal">
                <Grid Width="1140">
                    <Label Content="{Binding ID}" Width="40" FontSize="11" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Margin="5, 0, 0, 0" Foreground="{StaticResource FG_Component}"/>
                    <Label Content="{Binding Result}" Width="45" Margin="5, 18, 0, 0" HorizontalAlignment="Left" HorizontalContentAlignment="Left" FontSize="16"
                           Foreground="{Binding Result, Converter={StaticResource WinLoseColorConverter}}"/>

                    <Label Content="{Binding Patch}" Width="40" FontSize="12" HorizontalAlignment="Right" HorizontalContentAlignment="Left" Margin="5, 0, 110, 0" Foreground="{StaticResource FG_Component}"/>

                    <Label Content="{Binding Date}" Width="120" FontSize="12" HorizontalContentAlignment="Right" Margin="10, 0, 0, 0" Foreground="{StaticResource FG_Component}" HorizontalAlignment="Right"/>

                    <StackPanel Margin="50, 5, 0, 0" Orientation="Horizontal">
                        <Border BorderBrush="#585858" BorderThickness="1, 0, 0, 0" />
                        <Label Content="{Binding MyElo}" Width="50" Margin="10, 0, 0, 0" Foreground="#DFDFDF" FontWeight="DemiBold"/>
                        <Label Content="{Binding OpponentElo}" Width="50" Margin="10, 0, 0, 0" Foreground="#DFDFDF" FontWeight="DemiBold"/>

                        <Border BorderBrush="#585858" BorderThickness="1, 0, 0, 0" Margin="0, 0, 0, 35"/>

                        <Label Content="{Binding Opponent}" Width="160" Margin="10, 0, 0, 0" HorizontalContentAlignment="Center" FontSize="14" Foreground="#DFDFDF" />
                        <Border BorderBrush="#585858" BorderThickness="1, 0, 0, 0" Margin="0, 0, 20, 35"/>
                        <Image Source="{Binding OppChar1, Converter={StaticResource IconConverter}}" Height="48" Width="48" Margin="0, -4, 0, 4"/>
                        <Label Content="{Binding OppChar1}" Width="140" Margin="10, 5, 0, 0" Foreground="{StaticResource FG_Component}" HorizontalContentAlignment="Left" />
                        <Image Source="{Binding OppChar2, Converter={StaticResource IconConverter}}" Height="48" Width="48" Margin="0, -4, 0, 4"/>
                        <Label Content="{Binding OppChar2}" Width="140" Margin="10, 5, 0, 0" Foreground="{StaticResource FG_Component}" HorizontalContentAlignment="Left" />
                        <Image Source="{Binding OppChar3, Converter={StaticResource IconConverter}}" Height="48" Width="48" Margin="0, -4, 0, 4"/>
                        <Label Content="{Binding OppChar3}" Width="140" Margin="10, 5, 0, 0" Foreground="{StaticResource FG_Component}" HorizontalContentAlignment="Left" />
                        <Border BorderBrush="#585858" BorderThickness="1, 0, 0, 0" Margin="30, 0, 0, 35"/>
                    </StackPanel>

                    <Grid Margin="50, 15, 0, 0">
                        <Label Content="{Binding Notes}" FontSize="12" Width="150" Margin="20, 10, 0, 0" Foreground="{StaticResource FG_Control_Disabled}" HorizontalAlignment="Right" HorizontalContentAlignment="Right"/>
                    </Grid>

                    <Border BorderBrush="#585858" BorderThickness="0, 0, 0, 1" Margin="20, 0, 10, 0"/>
                </Grid>
            </StackPanel>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="MetaResultTemplate" DataType="{x:Type data:CharacterMetadata}">
        <Grid Height="65">
            <StackPanel Orientation="Horizontal">
                <Grid Width="455">
                    <Border BorderBrush="#555" BorderThickness="0, 1, 0, 0" />
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Character, Converter={StaticResource IconConverter}}" Height="32" Width="32" Margin="0, 0, 0, 0"/>
                        <Label Content="{Binding Character}" Width="110" VerticalContentAlignment="Center" Margin="10, 0, 0, 0"/>
                        <Border BorderBrush="#555" BorderThickness="0, 0, 1, 0" Margin="0, 20, 0, 20"/>
                        <Grid Width="100">
                            <Label Content="{Binding RecordString}" Width="80" Margin="0, 5, 0, 0"  HorizontalAlignment="Left" HorizontalContentAlignment="Center" FontSize="16" FontWeight="DemiBold"/>
                            <Label Content="{Binding TotalGames}" Margin="0, 0, 0, 5" VerticalAlignment="Bottom" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Width="80"
                                   Foreground="{StaticResource FG_Control_Disabled}" FontSize="13"/>
                        </Grid>
                        <Label Content="{Binding WinRatioString}" Width="100" Foreground="{Binding WinRatioString, Converter={StaticResource WinRatioToColor}}" VerticalAlignment="Center" FontSize="16" FontWeight="DemiBold"/>
                        <Label Content="{Binding WeightedData.AdjustedMatchupRating}" Foreground="{Binding WeightedData.AdjustedMatchupRating, Converter={StaticResource WinRatioToColor}}" Width="100" VerticalAlignment="Center" FontSize="16" FontWeight="DemiBold"/>
                        <Border BorderBrush="#555" BorderThickness="0, 0, 1, 0" Margin="0, 20, 0, 20"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="MetaResultTemplateShort" DataType="{x:Type data:CharacterMetadata}">
        <Grid Height="65">
            <StackPanel Orientation="Horizontal">
                <Grid Width="480">
                    <Border BorderBrush="#555" BorderThickness="0, 1, 0, 0" Margin="-10, 0, 0, 0" />
                    <StackPanel Orientation="Horizontal">
                        <Grid Width="100">
                            <Label Content="{Binding RecordString}" Width="80" Margin="0, 5, 0, 0"  HorizontalAlignment="Left" HorizontalContentAlignment="Center" FontSize="16" FontWeight="DemiBold"/>
                            <Label Content="{Binding TotalGames}" Margin="0, 0, 0, 5" VerticalAlignment="Bottom" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Width="80"
                                   Foreground="{StaticResource FG_Control_Disabled}" FontSize="13"/>
                        </Grid>
                        <Label Content="{Binding WinRatioString}" Width="100" Foreground="{Binding WinRatioString, Converter={StaticResource WinRatioToColor}}" VerticalAlignment="Center" FontSize="16" FontWeight="DemiBold"/>
                        <Label Content="{Binding WeightedData.AdjustedMatchupRating}" Foreground="{Binding WeightedData.AdjustedMatchupRating, Converter={StaticResource WinRatioToColor}}" Width="100" VerticalAlignment="Center" FontSize="16" FontWeight="DemiBold"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="AdjustedMatchupsTemplate" DataType="{x:Type data:WeightedCharacterMetadata}">
        <Grid Height="35">
            <StackPanel Orientation="Horizontal">
                <Grid Width="380">
                    <Border BorderBrush="#555" BorderThickness="0, 1, 0, 0" />
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Character, Converter={StaticResource IconConverter}}" Height="32" Width="32" Margin="0, 0, 0, 0"/>
                        <Label Content="{Binding Character}" Width="140" VerticalContentAlignment="Center" Margin="10, 0, 0, 0"/>
                        <Label Content="{Binding RecordClose}" Width="100"/>
                        <Label Content="{Binding RecordMedium}" Width="100"/>
                        <Label Content="{Binding RecordFar}" Width="100"/>
                        <Label Content="{Binding RecordUnranked}" Width="100"/>
                        <Label Content="{Binding AdjustedEloSeasonal}" Width="60"/>
                        <Label Content="{Binding AdjustedMUSeasonal}" Width="60"/>
                        <Label Content="{Binding AdjustedEloTotal}" Width="60"/>
                        <Label Content="{Binding AdjustedMUtotal}" Width="60"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </DataTemplate>
</ResourceDictionary>