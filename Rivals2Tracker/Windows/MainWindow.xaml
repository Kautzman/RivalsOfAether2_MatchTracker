<kwin:kWindow x:Class="Slipstream.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Slipstream"
        xmlns:controls="clr-namespace:Slipstream.Resources.Controls"
        xmlns:behaviors="clr-namespace:Slipstream.Resources.Behaviors"
        xmlns:kwin="clr-namespace:kWindows.Core"
        mc:Ignorable="d"
        Title="MainWindow" Height="1400" Width="2560"
        Style="{StaticResource BaseWindow}">
    <kwin:kWindow.DataContext>
        <local:MainWindow_VM />
    </kwin:kWindow.DataContext>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="8, 4, 8, 8" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Storyboard x:Key="ErrorTextFade">
            <ColorAnimation 
                Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)"
                To="{StaticResource FG_C_ErrorFaded}"
                Duration="0:0:3" />
        </Storyboard>
    </Window.Resources>
    <!-- TODO [high]: Pass on controls and style up common elements into a defined style -->
    <!-- TODO [low]: IsReadOnly and IsHitTestVisible behaviors instead of abusing MVVM so rudely -->
    <Grid>
        <DockPanel>
            <!-- Top Bar-->
            <Grid DockPanel.Dock="Top" Background="{StaticResource LGR_Banner}" Height="130" MouseDown="EnableDrag" Panel.ZIndex="1000">
                <Label Content="COMPLETED" Panel.ZIndex="1001" FontSize="54" Margin="230, 50, 0, 0" Width="400" HorizontalAlignment="Left" FontFamily="Franklin Gothic Demi Cond" Foreground="{StaticResource FG_Gold}" IsHitTestVisible="False"
                       Visibility="{Binding CompletedLabelVisibility}">
                    <Label.Effect>
                        <DropShadowEffect Color="Black" Direction="0" ShadowDepth="6" BlurRadius="1" Opacity="1" />
                    </Label.Effect>
                    <Label.RenderTransform>
                        <RotateTransform Angle="-10" />
                    </Label.RenderTransform>
                </Label>

                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Panel.ZIndex="1000">
                    <Grid Margin="145, 8, 0, 0">

                        <Label Content="Set My Tag..." Visibility="{Binding IsMyTagPhVisible}" IsHitTestVisible="False" FontStyle="Italic" FontSize="16" Margin="5, 15, 0, 0" Foreground="{StaticResource FG_Control_Disabled}"/>
                        <Label Content="Set My Elo..." Visibility="{Binding IsMyEloPhVisible}" IsHitTestVisible="False" FontStyle="Italic" FontSize="16" Margin="5, 0, 0, 23" VerticalAlignment="Bottom" Foreground="{StaticResource FG_Control_Disabled}" />

                        <TextBox Text="{Binding MyName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TabIndex="1" KeyDown="TextBox_KeyDown" FontFamily="Franklin Gothic Demi Cond" Background="Transparent"
                                 Height="60" VerticalAlignment="Top" Style="{StaticResource MatchInformationTextBoxes}" />
                        <TextBox Text="{Binding MyElo, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TabIndex="2" KeyDown="TextBox_KeyDown" Margin="0, 0, 0, 10" FontFamily="{StaticResource AfterDisaster}" Background="Transparent"
                                 Foreground="{StaticResource FG_WhiteLabel}" Height="50" VerticalAlignment="Bottom" FontSize="42" Style="{StaticResource MatchInformationTextBoxes}" IsReadOnly="{Binding IsMatchTextBoxesReadOnly}" IsHitTestVisible="{Binding IsMatchTextBoxesHitTestable}"/>
                    </Grid>

                    <Label Content="VS" FontFamily="Franklin Gothic Demi Cond" Foreground="{StaticResource FG_WhiteLabel}" FontSize="42" Margin="10, 0, 0, 0" VerticalAlignment="Center" />

                    <Grid Margin="0, 8, 0, 0">
                        <!-- Button For Main -->
                        <Button Name="ImageSelectorButton" Grid.Row="0" Width="68" Height="68" Margin="15, 20, 20, 20" HorizontalAlignment="Left" Command="{Binding ShowFlyoutCommand}"
                                BorderThickness="2" BorderBrush="Transparent" Style="{StaticResource MainImageButtonStyle}">
                            <Button.Background>
                                <ImageBrush ImageSource="{Binding SelectedImagePath}" Stretch="UniformToFill"/>
                            </Button.Background>
                        </Button>

                        <!-- Flyout Popup For Main -->
                        <Popup Name="ImageFlyout" Grid.Row="0" PlacementTarget="{Binding ElementName=ImageSelectorButton}" Placement="Bottom" IsOpen="{Binding IsOppFlyoutOpen}" StaysOpen="False"
                            AllowsTransparency="True" PopupAnimation="Slide">
                            <Border Background="{StaticResource BG_DarkBlue}" BorderBrush="{StaticResource FG_BlueLabel}" BorderThickness="1" CornerRadius="0" Padding="0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="5" Opacity="0.3"/>
                                </Border.Effect>

                                <StackPanel>
                                    <Label Content="Set Opponent Main" FontSize="22" FontFamily="Franklin Gothic Demi Cond" HorizontalAlignment="Center" Foreground="{StaticResource FG_BlueLabel}"/>
                                    <Border BorderThickness="0, 0, 0, 1" Margin="20, 0, 20, 0" BorderBrush="White"/>
                                    <ItemsControl ItemsSource="{Binding AvailableCharacters}" ItemTemplate="{StaticResource CharacterItemTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Width="280" Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </Popup>

                        <!-- Button For Secondary -->
                        <Grid Margin="62, 47, 0, 0">
                            <Button Name="ImageSelectorButtonSecondary" Grid.Row="0" Width="36" Height="36" Margin="20" HorizontalAlignment="Left" Command="{Binding ShowSecondaryFlyoutCommand}"
                                    BorderThickness="2" BorderBrush="Transparent" Style="{StaticResource MainImageButtonStyle}">
                                <Button.Background>
                                    <ImageBrush ImageSource="{Binding SecondarySelectedImagePath, TargetNullValue=C:\\Users\\mkautzman\\Documents\\Projects\\RivalsOfAether2_MatchTracker\\Rivals2Tracker\\Resources\\CharacterIcons\\unknown.png}" Stretch="UniformToFill"/>
                                </Button.Background>
                            </Button>

                            <!-- Flyout Popup For Secondary -->
                            <Popup Name="ImageFlyoutSecondary" Grid.Row="0" PlacementTarget="{Binding ElementName=ImageSelectorButtonSecondary}" Placement="Bottom" IsOpen="{Binding IsSecondaryFlyoutOpen}" StaysOpen="False"
                                AllowsTransparency="True" PopupAnimation="Slide">
                                <Border Background="{StaticResource BG_DarkBlue}" BorderBrush="{StaticResource FG_BlueLabel}" BorderThickness="1" CornerRadius="0" Padding="0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Direction="315" ShadowDepth="5" Opacity="0.3"/>
                                    </Border.Effect>

                                    <StackPanel>
                                        <Label Content="Set Opponent Secondary" FontSize="22" FontFamily="Franklin Gothic Demi Cond" HorizontalAlignment="Center" Foreground="{StaticResource FG_BlueLabel}"/>
                                        <Border BorderThickness="0, 0, 0, 1" Margin="20, 0, 20, 0" BorderBrush="White"/>
                                        <ItemsControl ItemsSource="{Binding AvailableCharacters}" ItemTemplate="{StaticResource SecondaryCharacterItemTemplate}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Width="280" Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </Grid>

                        <Grid Margin="20, 0, 0, 0">
                            <!-- TODO: This whole pattern with the PH labels is nonsense.  This needs to be a usercontrol -->
                            <Label Content="Set Opponent Tag..." Visibility="{Binding IsOpponentTagPhVisible}" IsHitTestVisible="False" FontStyle="Italic" FontSize="16" Margin="105, 15, 0, 0" Foreground="{StaticResource FG_Control_Disabled}"/>
                            <Label Content="Set Opponent Elo..." Visibility="{Binding IsOpponentTagPhVisible}" IsHitTestVisible="False" FontStyle="Italic" FontSize="16" Margin="105, 0, 0, 23" VerticalAlignment="Bottom" Foreground="{StaticResource FG_Control_Disabled}"/>

                            <TextBox Text="{Binding OpponentName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TabIndex="3" KeyDown="TextBox_KeyDown" IsReadOnly="{Binding IsMatchTextBoxesReadOnly}" IsHitTestVisible="{Binding IsMatchTextBoxesHitTestable}"
                                     Margin="100, 0, 0, 0" FontFamily="Franklin Gothic Demi Cond" Background="Transparent" Height="60" VerticalAlignment="Top" Style="{StaticResource MatchInformationTextBoxes}" />

                            <TextBox Text="{Binding OpponentElo, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TabIndex="4" KeyDown="TextBox_KeyDown" IsReadOnly="{Binding IsMatchTextBoxesReadOnly}" IsHitTestVisible="{Binding IsMatchTextBoxesHitTestable}"
                                     Margin="100, 0, 0, 10" FontFamily="{StaticResource AfterDisaster}" Background="Transparent" Foreground="{StaticResource FG_WhiteLabel}" Height="50" VerticalAlignment="Bottom" FontSize="42" Style="{StaticResource MatchInformationTextBoxes}"/>
                        </Grid>
                    </Grid>
                </StackPanel>

                <Label Content="{Binding ActiveMatchStatusString, UpdateSourceTrigger=PropertyChanged}" Name="FocusDump" Focusable="True" FontFamily="Franklin Gothic Demi" Foreground="{StaticResource FG_WhiteLabel}" FontSize="32" Margin="0, 5, 0, 0" VerticalAlignment="Top" HorizontalAlignment="Center" />
                <Label Content="{Binding ActivityText, UpdateSourceTrigger=PropertyChanged}" FontFamily="Franklin Gothic Demi" FontSize="18" Margin="0, 45, 0, 0" VerticalAlignment="Top" HorizontalAlignment="Center" behaviors:ActivityFadeBehavior.EnableTextFadeActivity="true">
                    
                </Label>

                <StackPanel VerticalAlignment="Bottom">
                    <Border BorderBrush="{StaticResource FG_Gold}" BorderThickness="0, 3, 0, 0" VerticalAlignment="Bottom" />
                    <Border BorderBrush="{StaticResource FG_DarkGold}" BorderThickness="0, 3, 0, 0" VerticalAlignment="Bottom"/>
                    <Border BorderBrush="{StaticResource FG_DarkDarkGold}" BorderThickness="0, 1, 0, 0" VerticalAlignment="Bottom"/>
                </StackPanel>

                <Border BorderBrush="{StaticResource FG_DarkGold}" BorderThickness="2" Background="{StaticResource BG_DarkBlue}" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               CornerRadius="25" Margin="0, 0, 0, -30" Style="{StaticResource MatchResultButtonsBorder}">
                    <Border BorderBrush="{StaticResource FG_Gold}" BorderThickness="3" Background="{StaticResource BG_DarkBlue}" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               CornerRadius="20" Margin="0">
                        <Border BorderBrush="{StaticResource FG_Gold}" BorderThickness="1" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               CornerRadius="15" Margin="0" Loaded="OuterBorder_Loaded">
                            <Grid Style="{StaticResource MatchResultButtonsGrid}">
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="WIN" FontFamily="Franklin Gothic Demi Cond" BorderThickness="0" Background="{StaticResource BG_DarkBlue}"
                                            Foreground="{StaticResource FG_BlueLabel}" Padding="0, 0, 0, 12" Command="{Binding SetMatchWinCommand}" Style="{StaticResource MatchResultButton}"/>
                                    <Button Content="LOSE" FontFamily="Franklin Gothic Demi Cond" BorderThickness="3, 0, 3, 0" Background="{StaticResource BG_DarkBlue}"
                                            Foreground="{StaticResource FG_BlueLabel}" Padding="0, 0, 0, 12" Command="{Binding SetMatchLoseCommand}" Style="{StaticResource MatchResultButton}"/>
                                    <Button  FontFamily="Franklin Gothic Demi Cond" BorderThickness="0" Background="{StaticResource BG_DarkBlue}"
                                            Foreground="{StaticResource FG_BlueLabel}" Padding="0, 0, 15, 12" Command="{Binding SetMatchDiscardCommand}" Style="{StaticResource MatchResultButtonSmallText}">
                                        <Button.Content>
                                            <Label Content="{Binding CancelNewMatchButtonString}" Width="90" HorizontalContentAlignment="Center">
                                                <Label.Style>
                                                    <Style TargetType="Label" BasedOn="{StaticResource CancelStartMatchLabel}">
                                                        <Setter Property="Foreground" Value="{StaticResource FG_BlueLabel}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=IsMouseOver}" Value="True">
                                                                <Setter Property="Foreground" Value="{StaticResource BG_DarkBlue}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Label.Style>
                                            </Label>
                                        </Button.Content>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Border>
                </Border>

                <Grid HorizontalAlignment="Right" Margin="0, 0, 100, 0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <Label Content="Patch:" FontFamily="Franklin Gothic Demi" KeyDown="TextBox_KeyDown" FontStyle="Italic" Foreground="{StaticResource FG_BlueLabel}" FontSize="48" Margin="30, 0, 0, 0"  />
                        <TextBox Text="{Binding CurrentPatch, UpdateSourceTrigger=PropertyChanged}" FontFamily="{StaticResource AfterDisaster}" Background="Transparent"
                                 Foreground="{StaticResource FG_WhiteLabel}" FontSize="42" Height="60" Margin="10, 6, 20, 0" IsReadOnly="True" IsHitTestVisible="False"/>
                    </StackPanel>

                    <Label Content="Set Player Notes..." Visibility="{Binding IsNotesPhVisible}" IsHitTestVisible="False" FontStyle="Italic" FontSize="16"
                           Margin="0, 0, 40, 23" Width="135" HorizontalContentAlignment="Right" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="{StaticResource FG_Control_Disabled}"/>

                    <TextBox Text="{Binding ActiveMatchNotes}" TabIndex="5" KeyDown="TextBox_KeyDown" FontFamily="Franklin Gothic Demi" Background="Transparent" Height="60" Margin="0, 0, 20, 10"
                         VerticalAlignment="Bottom" HorizontalAlignment="Right" HorizontalContentAlignment="Right" Style="{StaticResource MatchInformationNotesTextBox}" IsReadOnly="{Binding IsMatchTextBoxesReadOnly}" IsHitTestVisible="{Binding IsMatchTextBoxesHitTestable}"/>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button x:Name="WindowMinimize" Style="{StaticResource WindowingButton}" Click="WindowMiminizeClick">
                        <Image Style="{StaticResource Windowing_Image}" Source="/Slipstream;component/Resources/Icons/Windowing/Minimize.png" />
                    </Button>

                    <Button x:Name="WindowMaximize" Style="{StaticResource WindowingButton}" Click="WindowMaximizeClick">
                        <Image Style="{StaticResource Windowing_MaxMinImage}"/>
                    </Button>

                    <Button x:Name="WindowClose" Style="{StaticResource WindowingButton_Close}" Click="WindowCloseClick">
                        <Image Style="{StaticResource Windowing_Image}" Source="/Slipstream;component/Resources/Icons/Windowing/Close.png" />
                    </Button>
                </StackPanel>

                <Button Width="68" Content="&#xE713;" FontSize="38" FontFamily="Segoe MDL2 Assets" Height="68" HorizontalAlignment="Right" Margin="0, 10, 18, 0"
                        Foreground="{StaticResource FG_BlueLabel}" Background="{StaticResource BG_DarkBlue}" BorderBrush="{StaticResource FG_BlueLabel}"
                        Command="{Binding ShowSettingsWindowCommand}" />
            </Grid>

            <!-- Bottom Content-->
            <Grid DockPanel.Dock="Bottom" Background="{StaticResource BG_DarkBlue}" Panel.ZIndex="1">
                <Image Source="pack://application:,,,/ImageResources/embossLogo2.png" Opacity="0.06" Width="900" />

                <DockPanel>
                    <Grid DockPanel.Dock="Left" Style="{StaticResource MatchContainerStyle}">
                        <Label Content="Recent Matches" FontFamily="Franklin Gothic Demi" FontSize="32" Margin="50, 25, 0, 0"/>

                        <!-- Recent Matches -->
                        <ListView ItemsSource="{Binding MatchResults}" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top"
                                  BorderThickness="0" Margin="10, 70, 0, 50" Style="{StaticResource RecentMatchesTemplateStyle}" >
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem" BasedOn="{StaticResource BaseListViewItem}">
                                    <Setter Property="Padding" Value="0, 2, 15, 0" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Grid>

                    <!-- Meta Results and Character Badges -->
                    <Grid Style="{StaticResource MetaResultsGrid}" DockPanel.Dock="Left">
                        <Label Content="{Binding ErrorText, UpdateSourceTrigger=PropertyChanged}" FontFamily="Franklin Gothic Demi" FontSize="18" Margin="0, -35, 0, 10" VerticalAlignment="Top"
                               Style="{StaticResource ErrorTextLabel}" HorizontalAlignment="Left" HorizontalContentAlignment="Center" behaviors:LabelFadeBehavior.EnableTextFade="true">
                        </Label>
                        <Grid Margin="0, 5, 0, 0">
                            <controls:ToggleSwitch x:Name="ResultTypeToggle" HorizontalAlignment="Left" VerticalAlignment="Top" OffContent="Seasonal"
                                OnContent="Lifetime" IsOn="{Binding ShowLifetimeResults, Mode=TwoWay}" Style="{StaticResource MetaResultsToggle}" />
                        </Grid>

                        <ListView Name="MetaResultsListView" ItemsSource="{Binding DisplayedCharacterResults.CharacterData}" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top"
                                BorderThickness="0" Margin="0, 50, 0, 0" SelectedItem="{Binding ActiveMatchSeason_CharacterData}" Style="{StaticResource MetaResultsTemplateStyle}">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="3" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem" BasedOn="{StaticResource BaseListViewItem}">

                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Grid>

                    <!-- Per Character Matches / Active Match Notes -->
                    <Grid DockPanel.Dock="Right">
                        <Label Content="Match History" FontFamily="Franklin Gothic Demi" FontSize="32" Margin="75, 25, 0, 0"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Height="40" Margin="0, 30, 49, 0">
                            <Button Content="Rivals" ToolTip="Show Per Rivals Match History" Height="40" FontSize="24" Width="140" FontFamily="Franklin Gothic Demi" BorderThickness="2" Style="{StaticResource RivalsButtonStyle}" Command="{Binding ToggleRivalsMatchHistoryCommand}"/>
                            <Button Content="Players" ToolTip="Show Per Player Match History" Height="40" FontSize="24" Width="140" FontFamily="Franklin Gothic Demi" BorderThickness="2" Style="{StaticResource PlayersButtonStyle}" Command="{Binding TogglePlayersMatchHistoryCommand}"/>
                        </StackPanel>

                        <Grid Visibility="{Binding RivalsMatchHistoryViewVisibility}">
                            <ListView ItemsSource="{Binding ActiveMatchHistory.MatchResults}" Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Top"
                                      BorderThickness="0" Margin="0, 70, 10, 50" Grid.Column="2" Style="{StaticResource RecentMatchesTemplateStyle}">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem" BasedOn="{StaticResource BaseListViewItem}">
                                        <Setter Property="Padding" Value="0, 2, 15, 0" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </Grid>

                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="{Binding PlayerMatchHistoryViewVisibility}" Margin="0, 70, 10, 50">
                            <Grid Margin="10, 20, 0, 0">

                                <StackPanel Orientation="Horizontal" Margin="25, 10, 0, 0">
                                    <Button Content="&#xF78B;" FontSize="24" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource FG_WhiteLabel}" Margin="0, 10, 0, 0" VerticalAlignment="Top" 
                                            Height="40" Width="40" Background="{StaticResource BG_DarkBlue}" Padding="3" BorderBrush="{StaticResource FG_BlueLabel}" BorderThickness="2" Command="{Binding UpdateOpponentMatchHistoryCommand}"/>
                                    <TextBox Text="{Binding MatchHistoryOpponentTagSearch, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="5, 10, 0, 0" FontFamily="Franklin Gothic Demi Cond" Background="Transparent" Height="40" Width="240" VerticalAlignment="Top" BorderBrush="{StaticResource FG_BlueLabel}" 
                                         BorderThickness="0, 0, 0, 2" FontSize="26" HorizontalAlignment="Left">
                                        <TextBox.InputBindings>
                                            <KeyBinding Key="Return" Command="{Binding UpdateOpponentMatchHistoryCommand}" />
                                        </TextBox.InputBindings>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Margin="350, 20, 0, 0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
                                    <Label Content="{Binding MatchHistoryOpponent.WinsSeasonal}" FontFamily="Franklin Gothic Demi" FontSize="32"/>
                                    <Label Content=" - " FontFamily="Franklin Gothic Demi" FontSize="32"/>
                                    <Label Content="{Binding MatchHistoryOpponent.LosesSeasonal}" FontFamily="Franklin Gothic Demi" FontSize="32"/>
                                </StackPanel>

                                <StackPanel Margin="0, 20, 40, 0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Height="40">
                                    <Button Content="&#xF0E3;" ToolTip="Show Player Notes" Height="40" FontSize="24" Width="70" FontFamily="Segoe MDL2 Assets" BorderThickness="2" Style="{StaticResource PlayerNotesStyle}" Command="{Binding TogglePlayerNotesCommand}"/>
                                    <Button Content="&#xE8FD;" ToolTip="Show Player Match Record" Height="40" FontSize="24" Width="70" FontFamily="Segoe MDL2 Assets" BorderThickness="2" Style="{StaticResource PlayerMatchHistoryStyle}" Command="{Binding TogglePlayerMatchesCommand}"/>
                                </StackPanel>

                                <Grid Margin="20, 100, 20, 0">
                                    <!-- Player Notes-->
                                    <Grid Visibility="{Binding PlayerNotesVisibility}">
                                        <Label Content="{Binding MatchHistoryOpponent.NotesLabel, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" FontSize="20"/>
                                    </Grid>

                                    <!-- Player Match History-->
                                    <Grid Visibility="{Binding PlayerMatchesVisibility}">
                                        <ListView ItemsSource="{Binding MatchHistoryOpponentMatches.MatchResults}" Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Top"
                                                BorderThickness="0" Margin="0, 0, 0, 50" Grid.Column="2" Style="{StaticResource RecentMatchesTemplateStyle}">
                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="ListViewItem" BasedOn="{StaticResource BaseListViewItem}">
                                                    <Setter Property="Padding" Value="0, 2, 15, 0" />
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                        </ListView>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </DockPanel>
            </Grid>
        </DockPanel>

        <Border BorderBrush="{StaticResource GR_DarkBlue}" BorderThickness="4" Margin="15, 15, 0, 0" Width="128" Height="128" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Border BorderBrush="{StaticResource FG_Gold}" BorderThickness="4">
                <Grid>
                    <!-- Button For Main -->
                    <Button Name="MySelectorButton" Grid.Row="0" Width="120" Height="120" Margin="0, 0, 0, 0" HorizontalAlignment="Left" Command="{Binding ShowMyFlyoutCommand}"
                                BorderThickness="2" BorderBrush="Transparent" Style="{StaticResource MainImageButtonStyle}" Panel.ZIndex="1000">
                        <Button.Background>
                            <ImageBrush ImageSource="{Binding MySelectedCharPortrait}" Stretch="UniformToFill" />
                        </Button.Background>
                    </Button>

                    <!-- Flyout Popup For My Main -->
                    <Popup Name="MyFlyout" Grid.Row="0" PlacementTarget="{Binding ElementName=MySelectorButton}" Placement="Bottom" IsOpen="{Binding IsMyFlyoutOpen}" StaysOpen="False"
                            AllowsTransparency="True" PopupAnimation="Slide">
                        <Border Background="{StaticResource BG_DarkBlue}" BorderBrush="{StaticResource FG_BlueLabel}" BorderThickness="1" CornerRadius="0" Padding="0">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Direction="315" ShadowDepth="5" Opacity="0.3"/>
                            </Border.Effect>

                            <StackPanel>
                                <Label Content="Set My Character" FontSize="22" FontFamily="Franklin Gothic Demi Cond" HorizontalAlignment="Center" Foreground="{StaticResource FG_BlueLabel}"/>
                                <Border BorderThickness="0, 0, 0, 1" Margin="20, 0, 20, 0" BorderBrush="White"/>
                                <ItemsControl ItemsSource="{Binding AvailableCharacters}" ItemTemplate="{StaticResource MyCharacterItemTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Width="280" Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>
            </Border>
        </Border>
    </Grid>
</kwin:kWindow>
